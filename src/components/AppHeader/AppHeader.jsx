import React from "react";
import { Layout } from "antd";
const { Header } = Layout;

const AppHeader = (props) => <Header>Header</Header>;

AppHeader.propTypes = {
  // bla: PropTypes.string,
};

AppHeader.defaultProps = {
  // bla: 'test',
};

export default AppHeader;
